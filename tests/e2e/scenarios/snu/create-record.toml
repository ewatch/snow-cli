name = "snu-create-record"
description = "Create a record through the active SN-Utils browser session (snu CRUD: create)"
requires = ["sn-utils-bridge", "credentials"]

[[setup]]
description = "Create the scenario profile (instance context; bridge provides the session)"
args = ["profile", "add", "e2e-scenario", "--instance", "$SNOW_E2E_INSTANCE_URL", "--auth-method", "basic", "--username", "$SNOW_E2E_USERNAME"]

[command]
args = ["snu", "create-record", "incident", "--data", "{\"short_description\":\"snow-cli e2e scenario: snu-create-record\"}", "--profile", "e2e-scenario", "--output", "json"]
capture = { sys_id = ".sys_id | select(type == \"string\" and test(\"^[0-9a-f]{32}$\"))" }

[expect]
exit_code = 0
json_field_present = [
  "(.sys_id | type == \"string\") and (.sys_id | test(\"^[0-9a-f]{32}$\"))",
  ".record.sys_id == .sys_id",
  ".record.tableName == \"incident\"",
  ".record.field == \"short_description\"",
  ".record.content == \"snow-cli e2e scenario: snu-create-record\"",
]

[[cleanup]]
description = "Delete the record via the browser session"
args = ["snu", "delete-record", "incident", "--sys-id", "{{sys_id}}", "--profile", "e2e-scenario"]
allow_failure = true

[[cleanup]]
description = "Remove the scenario profile"
args = ["profile", "remove", "e2e-scenario", "--yes"]
allow_failure = true
